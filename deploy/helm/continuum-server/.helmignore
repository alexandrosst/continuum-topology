# Patterns to ignore when building packages.
.DS_Store
.git/
.gitignore
.vscode/
.idea/
*.swp
*.bak
*.tmp
*.orig
# CI value combinations are for `helm lint --values ci/x.yaml`, not for the package.
ci/
